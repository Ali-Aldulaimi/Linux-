#include <stdio.h>
#include "sigmoid.h"

int main (int argc, char *argv[]) {
	float x;
	float sd = sigmoid(x);
       sigmoid_dtv(sx);	
printf ("inswert an input\n");
	scanf ("%f",&x);
	printf (" the sigmoid of %f is %f .\n",x,sigmoid(x));
	printf (" the derviative value is %f . \n";sigmoid_dtv(y);

	return 0;
}
